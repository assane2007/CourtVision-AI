[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build:shared && npm run build:ai && npm run build:api"

[deploy]
startCommand = "npm start --workspace=@courtvision/api"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/health"
healthcheckTimeout = 30
numReplicas = 1
